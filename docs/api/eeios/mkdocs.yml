# Generated by xojo-docgen. Each project is a standalone MkDocs site.
# Build with: mkdocs build -f ../docs/api/eeios/mkdocs.yml

site_name: EEiOS — API
docs_dir: content
site_dir: ../../api-published/eeios
extra:
  asset_version: ec82b71cea764351

# Inherit the selected template configuration copied into this project.
INHERIT: mkdocs.base.yml

# Per-project site URL. Leave commented for root hosting; uncomment and set for
# subpath hosting (e.g. GitHub Pages project sites).
# site_url: https://<user>.github.io/<repo>/eeios/

use_directory_urls: true
